ingress:
  enabled: false
api:
  image:
    tag: '25'
  annotations: |
    "consul.hashicorp.com/connect-inject": "true"
    "consul.hashicorp.com/transparent-proxy": "false"
    "geometrylabs.io/logging": "true"
  kafka:
    topics:
      blocks: blocks-raw
      transactions: transactions-raw
      logs: logs-raw
worker:
  annotations: |
    "geometrylabs.io/logging": "true"
  image:
    tag: '25'
  kafka:
    topics:
      blocks: blocks-raw
      transactions: transactions-raw
      logs: logs-raw
db:
  sslMode: require
redis:
  host: "10.101.62.32"
#  sentinel:
#    clientMasterName: mymaster
#    clientMode: true
